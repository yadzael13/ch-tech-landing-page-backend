fastapi==0.141.1
uvicorn[standard]==0.52.1

sqlalchemy[asyncio]==2.0.51
aiomysql==0.2.0
# Required by aiomysql/PyMySQL to complete the RSA handshake for MySQL 8's
# default caching_sha2_password auth plugin — without it, every connection
# fails at handshake, not query time.
cryptography==50.0.0
alembic==1.18.5
pydantic-settings==2.14.2
bcrypt==5.0.0
pyjwt==2.13.0
email-validator==2.3.0
redis==8.1.0
httpx==0.28.1
